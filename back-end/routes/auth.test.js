describe("POST /login route", () => {
	it("should return object containing info on user if password is correct", () => {});
});
describe("POST /register route", () => {
	it("should return an object containing a token if the user registers successfully", () => {});
});
